<div class="row">
    <div class="col-sm-12 jumbotron">
      <h2><%= @user.username %></h2>
      <hr />
      <h4><%= link_to @team.name, team_path(@team.name) %></h4>
    </div>
</div>

<%= render partial: "shared/user_dash", locals: {quizzes: @quizzes} %>
