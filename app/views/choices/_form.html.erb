<%= form_with(model: choice, local: true) do |form| %>
  <% if choice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(choice.errors.count, "error") %> prohibited this choice from being saved:</h2>

      <ul>
      <% choice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :choice_body %>
    <%= form.text_area :choice_body, id: :choice_body %>
  </div>

  <div class="field">
    <%= form.label :correct %>
    <%= form.check_box :correct, class: :choice_correct %>
  </div>

  <div class="field">
    <%= form.label :quiz_id %>
    <%= form.text_field :quiz_id, id: :choice_quiz_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
